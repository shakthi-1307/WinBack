# One command to a running console:  docker compose up
services:
  winback:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    command: uvicorn backend.api.app:app --host 0.0.0.0 --port 8000
